<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Books Taken History - Luminara Library</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f0f0f0;
        }

        .container {
            padding: 20px;
            margin: auto;
            width: 80%;
            background-color: #fff;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h1 {
            text-align: center;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }

        table, th, td {
            border: 1px solid #ddd;
        }

        th, td {
            padding: 10px;
            text-align: left;
        }

        th {
            background-color: #0078e8;
            color: white;
        }

        .taken {
            color: yellow;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Books Taken History</h1>
        <table>
            <thead>
                <tr>
                    <th>Student Name</th>
                    <th>Book Name</th>
                    <th>Due Date</th>
                </tr>
            </thead>
            <tbody>
                {{#each booksTaken}}
                <tr>
                    <td>{{this.studentName}} ({{this.studentId}})</td>
                    <td>{{this.bookName}} ({{this.bookId}})</td>
                    <td>{{this.dueDate}}</td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
</body>
</html>
