<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Library History - Luminara Library</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f0f0f0;
        }

        .container {
            padding: 20px;
            margin: auto;
            width: 80%;
            background-color: #fff;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h1 {
            text-align: center;
        }

        h2 {
            color: #0078e8;
            margin-top: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }

        table, th, td {
            border: 1px solid #ddd;
        }

        th, td {
            padding: 10px;
            text-align: left;
        }

        th {
            background-color: #0078e8;
            color: white;
        }

        .overdue td {
            color: red;
        }

        .taken td {
            color: black;
        }

        .returned td {
            color: green;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>History</h1>

        <h2>Overdue Books</h2>
        <table>
            <thead>
                <tr>
                    <th>Book Name</th>
                    <th>Student Name</th>
                    <th>Fine</th>
                </tr>
            </thead>
            <tbody>
                {{#each overdueBooks}}
                <tr class="overdue">
                    <td>{{this.bookName}} ({{this.bookId}})</td>
                    <td>{{this.studentName}} ({{this.studentId}})</td>
                    <td>₹{{this.fine}}</td>
                </tr>
                {{/each}}
            </tbody>
        </table>

        <h2>Books Taken</h2>
        <table>
            <thead>
                <tr>
                    <th>Book Name</th>
                    <th>Student Name</th>
                </tr>
            </thead>
            <tbody>
                {{#each booksTaken}}
                <tr class="taken">
                    <td>{{this.bookName}} ({{this.bookId}})</td>
                    <td>{{this.studentName}} ({{this.studentId}})</td>
                </tr>
                {{/each}}
            </tbody>
        </table>

        <h2>Books Returned</h2>
        <table>
            <thead>
                <tr>
                    <th>Book Name</th>
                    <th>Student Name</th>
                </tr>
            </thead>
            <tbody>
                {{#each booksReturned}}
                <tr class="returned">
                    <td>{{this.bookName}} ({{this.bookId}})</td>
                    <td>{{this.studentName}} ({{this.studentId}})</td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
</body>
</html>
